fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
python-multipart>=0.0.9
pypdf>=4.0
pytest>=8.0

# PostgreSQL + pgvector（数据库与向量检索）
psycopg[binary]>=3.1
pgvector>=0.2

# CodeCrucible 远程判题（JWT 鉴权）
PyJWT>=2.8

# 可选：扫描版 PDF OCR（体积较大，见 requirements-ocr.txt）
