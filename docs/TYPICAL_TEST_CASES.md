# 典型问题测试案例与准确性论证

> 说明：以下用例全部来自真实运行结果——检索使用 bge-small-zh-v1.5 真实 Embedding +
> 混合检索（语义 0.75 + 词法 0.25 + 关键词重叠 + 元数据加权），回答由 DeepSeek 生成，
> 引用为系统实际检索到的知识库来源。运行时间：2026-08-27。

---

## 用例一：动态规划（知识库命中，中文讲义引用）

**问题**：动态规划的基本思想与最优子结构

**检索引用（Top3）**

| 等级 | 来源 | 主题 | 相似度 |
| --- | --- | --- | --- |
| [S] | 动态规划：从背包到编辑距离（《算法竞赛入门经典》第 9 章） | 动态规划 | 0.867 |
| [S] | 离散数学.pdf（Rosen，动态规划与生物信息学章节） | — | 0.715 |
| [S] | Textbook Algorithm Design.pdf（动态规划复杂度讨论） | — | 0.685 |

**AI 回答（摘要）**

> 根据[1]，动态规划适用于具有**最优子结构**与**重叠子问题**的问题。核心是定义状态
> （如 0/1 背包 `dp[i][w]` 表示前 i 个物品在容量 w 下的最大价值）、写出状态转移方程
> `dp[i][w]=max(dp[i-1][w], dp[i-1][w-wi]+vi)`，并通过记忆化或自底向上填表避免重复
> 计算，可用滚动数组优化空间……常见错误包括状态定义不清或遗漏边界初始化。

**与标准依据对照**：答案与《算法竞赛入门经典》第 9 章“动态规划”章节一致（最优子结构、
重叠子问题、状态转移、滚动数组优化均为教材原内容）。

**局限**：引用 [2][3] 涉及生物信息学/经济学应用场景，但讲义未展开这些领域的转移方程，
AI 已主动标注“参考内容不足”，未编造细节。

---

## 用例二：信号量与进程同步（知识库命中，操作系统教材引用）

**问题**：信号量如何实现进程同步与互斥

**检索引用（Top2）**

| 等级 | 来源 | 主题 | 相似度 |
| --- | --- | --- | --- |
| [S] | 进程同步：信号量与经典问题（《现代操作系统》第 2 章整理） | 同步互斥 | 0.793 |
| [S] | 进程、线程与调度（《计算机操作系统》汤小丹） | 进程管理 | 0.678 |

**AI 回答（摘要）**

> 信号量通过 P(wait)/V(signal) 两个原子操作实现：互斥用初值为 1 的互斥信号量
> `mutex`，进入临界区前 P(mutex)、离开后 V(mutex)；同步用同步信号量，如生产者-消费者
> 问题中 `empty`（初值为缓冲区大小）与 `full`（初值为 0）强制生产/消费按序交替……
> 死锁需满足互斥、占有并等待、不可剥夺、循环等待四个必要条件[1]。

**与标准依据对照**：P/V 操作语义、临界区三原则、生产者-消费者三信号量方案与
《现代操作系统》第 2 章内容一致。

**局限**：讲义未含信号量底层阻塞队列实现细节，AI 明确提示需要补充操作系统原理教材，
未用通用知识冒充教材内容。

---

## 用例三：婚姻匹配（跨语言命中英文教材，暴露检索局限）

**问题**：婚姻匹配问题是什么，稳定婚姻如何求解

**检索引用（Top3）**

| 等级 | 来源 | 相似度 |
| --- | --- | --- |
| [S] | Textbook Algorithm Design.pdf（Stable Matching 章节） | 0.757 |
| [S] | Textbook Algorithm Design.pdf（稳定匹配一般化讨论） | 0.705 |
| [S] | 离散数学.pdf（Rosen，稳定匹配算法习题） | 0.703 |

**AI 回答（摘要）**

> 参考内容提到了稳定匹配（stable matching）、完美匹配、不稳定性等概念，但切片内容不完整，
> 未系统解释婚姻匹配问题背景与 Gale-Shapley 算法步骤；参考内容不足以完整回答……

**与标准依据对照**：检索确实命中了 Algorithm Design 的 Stable Matching 章节（跨语言
中文提问 → 英文教材命中），来源正确、可追溯。

**局限（重要）**：
1. PDF 文本层切分导致切片从句子中间开始（如 “arriages with no instabilities”），
   上下文不完整；
2. 顶部切片拼出的“知识库参考”信息密度不足，DeepSeek 选择诚实说明“参考内容不足”
   而不是编造——这符合“宁可说不知道，不可假装引用”的产品原则；
3. 后续优化方向：切分时按句子边界/标题对齐，或对命中切片做拼接摘要后再交给 LLM。

---

## 用例四：知识库外问题（未命中，明确回退模型自身知识）

**问题**：量子计算中的叠加态是什么

**检索结果**：知识库未检索到相关依据（grounded = False，confidence = 0）

**AI 回答（开头）**

> 该回答未找到平台知识库依据，以下为模型自身知识。
>
> 叠加态是指量子系统可同时处于多个可能状态的线性组合……`|ψ⟩ = α|0⟩ + β|1⟩`，
> 测量时以 `|α|²` 概率坍缩到 |0⟩……n 个量子比特可同时表示 2ⁿ 种基态叠加……

**论证**：系统没有因为“语义上有点像”就伪造知识库引用；回答以明确的免责声明开头，
界面同时展示“未命中知识库”状态，符合“内容可追溯、不假装引用”的赛题要求。

---

## 结论

- 4 个用例覆盖：中文讲义命中、中文-英文跨语言命中、命中但资料不足（诚实降级）、
  未命中（扩展知识回退）四种典型场景；
- 全部回答要么带可点击引用（[S]/[A]/[P] + 来源 + 相似度），要么明确标注“模型自身知识”；
- 检索评测（44 道正例 + 5 道负例）Recall@1=90.9%、Recall@5=97.7%、负例误报=0，
  详见 [RAG_EVAL_REPORT.md](./RAG_EVAL_REPORT.md)。
